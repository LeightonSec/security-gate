# Copy this file to .env and populate your values.
# .env is gitignored — never commit real credentials.
#
# security-gate has no required environment variables for scanning.
# Add any future config here.
