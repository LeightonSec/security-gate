# Runtime deps — mirrors pyproject [project.dependencies].
# Compile at the CI matrix FLOOR so marker-gated transitives survive:
#   uv pip compile --universal --python-version 3.10 --generate-hashes requirements.in -o requirements.txt
typer>=0.12.0
rich>=13.0.0
tomli>=2.0.0; python_version < '3.11'
httpx>=0.27.0
